# Assets

Place your resume here as **`Muthukumar-B-Resume.pdf`** so the download button on the site picks it up:

```
assets/Muthukumar-B-Resume.pdf
```

The site references this exact filename in the header and mobile menu — if you save it with a different name, update both `<a href="assets/...">` lines in `index.html`.
